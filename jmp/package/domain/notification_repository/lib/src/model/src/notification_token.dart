/// {@template notification_token}
/// A class which represents a notification token.
/// {@endtemplate}
class NotificationToken {
  /// {@macro notification_token}
  NotificationToken(this.token);

  /// The actual token
  final String token;
}
